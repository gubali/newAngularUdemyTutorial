import { CustomStructureDirectiveDirective } from './custom-structure-directive.directive';

describe('CustomStructureDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomStructureDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
